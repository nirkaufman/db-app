import { Component } from '@angular/core';
import {LowPipe} from './utils/low.pipe';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
})
export class AppComponent {
  title = 'app';
}
